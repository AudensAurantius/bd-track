"""Dolt server lifecycle — list, stop, idle-stop subcommands."""

# Section: _bd_dolt_in helper
# Section: cmd_servers (list registered repos and Dolt server status)
# Section: cmd_server_stop (graceful stop for one or all registered repos)
# Section: cmd_idle_stop (stop servers idle longer than threshold)
# Section: _server_log_mtime, _last_activity (idle detection helpers)
