"""Foundation helpers used across modules.

Includes the subprocess wrapper, beads workspace discovery, the Rich-styled
argparse formatter, logging setup, interactive prompt helpers, activity
tracking for idle-stop, and shared path constants.
"""

# Section: imports
# Section: constants — REPOS_CONFIG, CLEANUP_STATE, CLEANUP_LOG, ACTIVITY_STATE, QUEUE_FILE, GLOBAL_SVC_DEFAULT
# Section: HelpFormatter
# Section: setup_logger
# Section: run() subprocess wrapper
# Section: find_beads_dir() workspace discovery
# Section: _is_interactive / _prompt / _confirm
# Section: activity tracking helpers
